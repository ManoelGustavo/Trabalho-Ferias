@{
    ViewBag.Title = "Cadastro";
}

<div class="container">
    <div class="card card-register mx-auto mt-4">
        <div class="card-header">Registro de Tarefa</div>
        <div class="card-body">
            <form action="@Url.Action("Store")" method="post">
                <div class="form-group">
                    <div class="form-row">
                        <div class="col-md-6">
                            <div class="form-label-group">
                                <label for="campo-usuario">Usuario</label>
                                <select id="campo-usuario" name="idUsuarioResponsavel" class="form-control" required="required">
                                    <option disabled="disabled" selected="selected">Selecione o Usuário Responsável</option>
                                    @foreach (Model.Usuario usuario in ViewBag.Usuarios)
                                    {
                                        <option value="@usuario.Id">@usuario.Nome</option>
                                    }
                                </select>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-label-group">
                                <label for="campo-projeto">Projeto</label>
                                <select id="campo-projeto" name="idProjeto" class="form-control" required="required">
                                    <option disabled="disabled" selected="selected">Selecione um Projeto</option>
                                    @foreach (Model.Projeto projeto in ViewBag.Projetos)
                                    {
                                        <option value="@projeto.Id">@projeto.Nome</option>
                                    }
                                </select>
                            </div>
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="form-row">
                            <div class="col-md-6">
                                <div class="form-label-group">
                                    <label for="campo-categoria">Categoria</label>
                                    <select id="campo-categoria" name="idCategoria" class="form-control" required="required">
                                        <option disabled="disabled" selected="selected">Selecione uma Categoria</option>
                                        @foreach (Model.Categoria categoria in ViewBag.Categorias)
                                        {
                                            <option value="@categoria.Id">@categoria.Nome</option>
                                        }
                                    </select>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-label-group">
                                    <label for="campo-titulo">Título</label>
                                    <input type="text" id="campo-titulo" class="form-control" required="required"
                                           placeholder="Título" autofocus="autofocus" name="titulo" />
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="form-row">
                            <div class="col-md-6">
                                <div class="form-label-group">
                                    <label for="campo-descricao">Descrição</label>
                                    <textarea cols="25" rows="3" id="campo-descricao" class="form-control" placeholder="Descrição"
                                              required="required" autofocus="autofocus" name="descricao"></textarea>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-label-group">
                                    <label for="campo-duracao">Duração</label>
                                    <input type="datetime" id="campo-duracao" class="form-control" placeholder="Duração"
                                           required="required" autofocus="autofocus" name="duracao" />
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <button class="btn btn-success col-md-12" type="submit">Registrar</button>
            </form>
            <div class="text-center">
                <a class="d-block small mt-3" href="@Url.Action("Index", "Tarefa")">Deseja alterar alguma tarefa já cadastrada ?</a>
            </div>
        </div>
    </div>
</div>